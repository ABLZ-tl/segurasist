# Bitácora compartida — Auditoría 10 sub-agentes

Append-only feed entre sub-agentes. Cada agente:
- Lee este archivo al iniciar (find existing cross-cutting concerns).
- Append una línea cuando encuentre un finding **cross-cutting** o **Critical/High**.
- Re-lee periódicamente para ver updates de otros agentes.

Formato:
```
[<área>] <YYYY-MM-DD HH:MM> <SEV> <file:line> — <descripción corta> // <impacto en otras áreas>
```

---

<!-- Append findings below this line -->
[A1] 2026-04-25 14:30 Medium auth.service.ts:233,318 — audit.record usa action='login' + resourceType='auth.otp.*' (sobrecarga del enum AuditAction). // A6: dashboards filtrando por action no distinguen OTP de admin login. Considerar extender enum.
[A1] 2026-04-25 14:30 Medium auth.service.ts:362-375,411-433 — AuthService.findInsuredByCurp inyecta PrismaBypassRlsService directo. // A2: si cambia shape de bypass client o rol DB, flujo OTP degrada silente a "CURP desconocido". Coordinar refactor a InsuredsService.findByCurpCrossTenant.
[A1] 2026-04-25 14:30 Low auth.controller.ts:45 — @TenantThrottle es no-op runtime sobre endpoint @Public sin req.tenant. // A6: ThrottlerGuard debe skip (no error) cuando req.tenant ausente; verificar.
[A1] 2026-04-25 14:30 Low auth.controller.ts:78-94 — GET /v1/auth/me expone `id` que es claims.sub (cognitoSub). // A7/A8: frontend puede asumir que es users.id (FK BD); documentar contrato.
[A1] 2026-04-25 14:30 High auth.service.spec.ts:95-97 — otpRequest/otpVerify en describe.skip; referencia test/integration/otp-flow.spec.ts que NO existe. // A10: marcar gap en inventario de tests; cobertura crítica RF-401 ausente.
[A1] 2026-04-25 14:30 Low app.module.ts:114-126 — JwtAuthGuard/RolesGuard NO registrados como APP_GUARD global; cada controller debe @UseGuards manualmente. // Cross-cutting: nuevo controller en Sprint 4 puede olvidar el guard. Recomendado registrar globalmente con @Public como escape.
[A4] 2026-04-25 16:50 Critical src/workers/pdf-worker.service.ts:316,357 — `hash` persistido es provisionalHash random, no SHA-256 del PDF; rompe contrato CertificateIssuedEvent // Afecta A5 (reports), A7 (admin UI cert detail), A10 (verify endpoint contract)
[A4] 2026-04-25 16:50 High src/infra/aws/ses.service.ts:154-155 — sendViaSes descarta tags/headers, X-Tag-cert no llega al webhook SES en prod // Afecta A6 (webhook ignora eventos), A9 (Terraform Configuration Set inútil sin tags)
[A4] 2026-04-25 16:50 High src/workers/pdf-worker.service.ts:282-294 — status 'failed' persistido como 'revoked' placeholder // Afecta A5 (reportes mezclan revoke manual con fallos), A2 (enum CertificateStatus necesita migración)
[A4] 2026-04-25 16:50 High src/modules/webhooks/ses-webhook.controller.ts:67 — POST /v1/webhooks/ses público sin throttle (DoS trivial) // Afecta A6 (throttler global)
[A4] 2026-04-25 16:50 High src/modules/webhooks/ses-webhook.controller.ts:76-85 — firma SNS no validada criptográficamente en prod (TODO Sprint 5) // Afecta A6 (hardening), A9 (necesita dep `sns-validator`)
[A4] 2026-04-25 16:50 Medium src/workers/mailpit-tracker.service.ts:85 — query `tag:cert` no matchea header X-Tag-cert seteado por SesService; tracker dev nunca sintetiza eventos // Afecta A10 (DX dev)
[A4] 2026-04-25 16:50 Medium src/modules/email/bounce-alarm.service.ts — provider declarado pero NUNCA invocado en runtime // Afecta A5 (reportes), A6 (alertas)
[A3] 2026-04-25 17:00 Critical src/workers/layout-worker.service.ts:137-140 — Dedup intra-archivo se aplica por chunk de 500 filas; CURPs duplicadas cross-chunk no se detectan en path async (>1k filas). // A10: cubrir con integration test de batch >1000 con duplicados cross-chunk.
[A3] 2026-04-25 17:00 Critical src/workers/insureds-creation-worker.service.ts:200-218 — `confirm()` no resetea rowsOk/rowsError; el worker incrementa encima → batch se marca `completed` tras la primera creación. // A4: `batch.completed` se emite prematuramente; PDF/cert workflow puede asumir batch listo cuando aún hay creations en cola.
[A3] 2026-04-25 17:00 High src/infra/aws/sqs.service.ts:20-26 — `MessageDeduplicationId` se envía a colas standard (LocalStack las crea sin .fifo); AWS real respondería InvalidParameterValue. // A4 (insured.created → PDF idempotencia), A9 (Terraform queue defs Sprint 5).
[A3] 2026-04-25 17:00 High src/modules/batches/batches.service.ts:478-484 — `queueUrlForCreations()` hace string-replace en SQS_QUEUE_LAYOUT en lugar de leer SQS_QUEUE_INSUREDS_CREATION del env. // A9 debe sumar la env var al schema y a Terraform Sprint 5.
[A3] 2026-04-25 17:00 Medium test/integration/batches-flow.spec.ts:1-34 — Header dice "integration con LocalStack" pero usa mocks in-memory; no arranca infra real. // A10: revisar naming/categorización de tests integration vs unit.
[A2] 2026-04-25 17:30 High segurasist-api/test/security/cross-tenant.spec.ts:241-287 — 23 it.todo HTTP-layer no implementados; PATCH/DELETE /v1/insureds/:id, POST /v1/batches/:id/confirm, GET /v1/reports/*, PATCH /v1/claims/:id sin blindaje de PR cross-tenant directo // impacta A3 (batches), A4 (certificates), A5 (insureds, reports), A6 (audit log).
[A2] 2026-04-25 17:30 High segurasist-api/prisma/rls/policies.sql:49-64 — tabla `exports` NO está en el array tenant-iso de policies.sql, sólo se crea en su migración 20260427_add_exports_table; re-aplicar policies.sql contra DB sin migrar omite exports // impacta A5 (reports/exports) y A9 (DevOps: bootstrap RLS).
[A2] 2026-04-25 17:30 Medium segurasist-api/prisma/schema.prisma:301 — drift entre `cognitoSub @unique` (schema) y partial UNIQUE index `WHERE cognito_sub IS NOT NULL` (migración 20260427_add_insured_cognito_sub) // impacta A10 (DX: `prisma migrate diff` en CI podría generar drift).
[A5] 2026-04-25 18:00 High src/infra/aws/sqs.service.ts:26 + insureds.service.ts:900 — `MessageDeduplicationId` enviado a `reports-queue` cola estándar; SQS lo ignora silente (mismo issue que A3-31 pero distinta cola). // A9 (Terraform: convertir reports-queue a .fifo o cambiar idempotencia DB-side); A6 (idempotencia POST /insureds/export).
[A5] 2026-04-25 18:00 High src/modules/insureds/export-rate-limit.guard.ts — Sin tests dedicados al guard de cap diario PII anti-abuse (10 exports/día). // A10 test coverage gap; A6 si cambia kill switch THROTTLE_ENABLED.
[A5] 2026-04-25 18:00 Medium insureds.service.ts:282-308,988-1015 + reports-worker.service.ts:307-322 — `buildInsuredsWhere` triplicado entre list/buildExportWhere/queryInsureds (drift risk). // A3 si batches comparte filters de búsqueda.
[A5] 2026-04-25 18:00 Medium insureds.controller.ts:115-119,173-177 + insureds.service.ts:631-637 — Patrón {ip,userAgent,traceId} extraction triplicado verbatim en 3 sitios. // A6: proponer @AuditCtx() decorator + interceptor automático.
[A5] 2026-04-25 18:00 Low insureds/cursor.ts + users/cursor.ts — codec idéntico duplicado base64url (createdAt,id). // A3 batches: si lista paginada keyset, promover a common/pagination/cursor.ts.
[A5] 2026-04-25 18:00 Medium reports.service.ts:262 — `getCoverageConsumedPct` retorna `trend: 0` siempre (no implementado pero contrato sugiere lo contrario). // A7/A8 frontend renderiza 0% como dato real; cambiar contrato a `trend?: number`.
[A5] 2026-04-25 18:00 Low reports.service.ts:86-97 — stubs `conciliation/usage/schedule` lanzan Error('pending') (500) en lugar de NotImplementedException (501). // A6/A10 semantics; el FE recibe 500 en endpoints declarados en RBAC matrix.
[A5] 2026-04-25 18:00 Low insureds.service.ts:673 + claims.service.ts:82 — cast `as unknown as Prisma.InsuredWhereInput` ya obsoleto post-migración 20260427_add_insured_cognito_sub. // Limpieza pura.
[A6] 2026-04-25 18:30 High src/modules/audit/audit-chain-verifier.service.ts:140-153 — `recomputeChainOkFromDb` (path source='both') sólo encadena prev_hash sin recomputar SHA; tampering coordinado en filas no mirroreadas pasa silencioso. // Impacta A2 (RLS bypass tampering), A9 (compliance audit).
[A6] 2026-04-25 18:30 High src/modules/audit/audit.controller.ts:63-77 — GET /v1/audit/verify-chain sin @Throttle/@TenantThrottle; full table scan + ListObjectsV2 + GetObject NDJSON puede DoS al cluster con creds super comprometidas. // Impacta A1 (auth abuse), A9 (cluster stability).
[A6] 2026-04-25 18:30 Medium src/common/interceptors/audit.interceptor.ts:11-30 vs src/common/utils/scrub-sensitive.ts:23-38 — listas SENSITIVE_KEYS duplicadas, depth distinta (8 vs 12); drift inevitable, claves nuevas en una utility no se reflejan en la otra. // Impacta A1/A4 (riesgo PII en logs).
[A6] 2026-04-25 18:30 Medium src/modules/audit/audit-writer.service.ts:110-117 — modo "pino-only" silente cuando DATABASE_URL_AUDIT ausente; sin alarma operativa, eventos críticos no persistidos. // Impacta A1 (auth audit), A2 (compliance ledger), A9 (env validation).
[A6] 2026-04-25 18:30 Medium src/modules/insureds/insureds.controller.ts:163 — POST /v1/insureds/export con @Throttle(user-IP) pero SIN @TenantThrottle; un IP rotativo puede saturar caps cross-bucket. Inconsistente vs batches.controller.ts:68. // Impacta A5.
[A6] 2026-04-25 18:30 Low src/app.module.ts:91 — THROTTLE_LIMIT_DEFAULT=10000 hard-coded en NODE_ENV=test enmascara regresiones de rate-limit en CI. // Impacta A10 (DX/test).
[A8] 2026-04-25 18:30 Critical apps/portal/app/api/proxy/[...path]/route.ts:2,13 — proxy importa SESSION_COOKIE (admin sa_session) en lugar de PORTAL_SESSION_COOKIE (sa_session_portal); insureds NUNCA obtienen Bearer en upstream → 401 en TODAS las llamadas autenticadas (useInsuredSelf, useCoveragesSelf, useCertificateMine, useCreateClaimSelf). // A1 (no es backend, FE bug routing); A5 (insureds self queries fallan); A4 (cert preview falla); A10 (gap test E2E proxy).
[A8] 2026-04-25 18:30 High apps/portal/app/api/proxy/[...path]/route.ts:12-44 — proxy del portal NO invoca checkOrigin() (regresión vs portal-otp-request/verify/logout que sí lo hacen). // A6 hardening; defensa en profundidad si middleware matcher cambia.
[A8] 2026-04-25 18:30 High apps/portal/next.config.mjs:20-31 — CSP no declara frame-src; iframe de certificate (S3 pre-signed *.amazonaws.com) bloqueado en prod por default-src 'self'. // A4 (UX preview certificate roto en prod); A9 (revisar parity CSP admin/portal).
[A8] 2026-04-25 18:30 Medium apps/portal/app/(app)/page.tsx:51 + lib/jwt.ts:50-63 — fallback greeting "Hola, insured.demo" cuando JWT solo trae email; cognito-local mock no emite given_name. // A1 (mock cognito-local agregar given_name); A4 (templates email pueden necesitar mismo claim).
[A8] 2026-04-25 18:30 Low apps/portal/components/layout/chat-fab.tsx:14-19 — placeholder Sprint 4 chatbot; aria-label engañoso ("Abrir asistente virtual" pero solo muestra toast). // Sprint 4 chatbot agente integration.
[A7] 2026-04-25 19:00 High apps/admin/app/api/auth/[...nextauth]/route.ts:10,68 — callback Cognito usa setSessionCookies de @segurasist/auth con sameSite='lax', bypassea hardening M6+L3 que admin/portal hicieron en cookie-config.ts (gap H-02 security audit). // Impacta A1 (auth: fixear packages/auth/src/session.ts), A8 (portal: misma exposure si usa el mismo callback).
[A7] 2026-04-25 19:00 High apps/admin/app/api/auth/[...nextauth]/route.ts:28,77,86 — logout sirve por GET (POST=GET aliased), no checa Origin. Permite CSRF logout vía <img src> o link malicioso. // Impacta A1 (auth: definir contrato POST-only para logout), A8 (portal: probable misma exposure).
[A7] 2026-04-25 19:00 Medium apps/admin/lib/cookie-config.ts ↔ apps/portal/lib/cookie-config.ts y apps/admin/lib/origin-allowlist.ts ↔ apps/portal/lib/origin-allowlist.ts — origin-allowlist byte-idéntico, cookie-config funcionalmente idéntico. // A8 (portal): consolidar en packages/security/ o packages/auth/.
[A7] 2026-04-25 19:00 Medium apps/admin/app/_components/mobile-drawer.tsx:83-91 — TenantSwitcher mobile es Select hard-coded ('mac'/'demo'); admin_segurasist en mobile no puede impersonar correctamente (banner amber desincronizado). // Impacta S3-08 security: bypass UX del tenant override.
[A7] 2026-04-25 19:00 Medium apps/admin/app/api/proxy/[...path]/route.ts:16 vs apps/admin/lib/auth-server.ts:6 + local-login/route.ts:24 + me/route.ts:14 — defaults API_BASE_URL inconsistentes (proxy → prod, otros → localhost); en prod sin env explícita, fetchMe falla pero proxy funciona. // Impacta A9 (DevOps env validation).
[A7] 2026-04-25 19:00 Low apps/admin/app/(app)/dashboard/{kpi-grid,dashboard-tables}.tsx — exportados pero NO importados por nadie (dead code post-S2-05). // Impacta A10 (test coverage targets stale).
[A9] 2026-04-25 19:30 Critical .github/workflows/ci.yml:367 — api-dast targeta /v1/openapi.json pero SwaggerModule no está montado en main.ts (verificado: 0 matches en src/). // A6/A10: gate ci-success bloqueará merges en cuanto OIDC desbloquee.
[A9] 2026-04-25 19:30 Critical segurasist-infra/global/iam-github-oidc/main.tf:226-238 — sólo existe tf_plan_dev role; terraform-plan.yml referencia tf-plan-{staging,prod} que no existen. // External GH-002+AWS-001 deben crearlos antes del primer apply.
[A9] 2026-04-25 19:30 High segurasist-infra/envs/{dev,staging,prod}/main.tf — módulo cloudwatch-alarm nunca instanciado; runbooks RB-001/002/004/005/007 mencionan alarmas inexistentes. // A6 confirma gap H-04 security audit; on-call ciego.
[A9] 2026-04-25 19:30 High segurasist-infra/envs/*/main.tf:364-369 — Terraform crea 4 SQS (layout/cert/email/reports) pero workers necesitan insureds-creation-queue (worker hace string-replace sobre SQS_QUEUE_LAYOUT). // A1/A3: drift entre LocalStack bootstrap y Terraform.
[A9] 2026-04-25 19:30 High .github/workflows/ci.yml — falta job docker build + Trivy scan de la imagen API antes del despliegue. // A6: imagen App Runner sin escaneo OS/lib (gap MVP-06 checklist).
[A9] 2026-04-25 19:30 High segurasist-infra/global/iam-github-oidc/main.tf:7-10,219-298 — deploy_*/tf_apply_* roles adjuntan PowerUserAccess/AdministratorAccess; viola least-privilege. // A6 cumplimiento; permissions_boundary mitiga pero no resuelve.
[A9] 2026-04-25 19:30 Medium segurasist-api/scripts/{local-up,cognito-local-bootstrap}.sh — pre-checks de psql/jq/aws/curl sin verificación de versión mínima. // A10: onboarding falla silente en máquinas con psql<14.
[A9] 2026-04-25 19:30 Medium segurasist-infra/global/organization/scps/deny-s3-object-lock-delete.json:14 — excepción AWSReservedSSO_AdminFullAccess_* contradice ADR-009. // A6: humano con SSO admin podría bypass; mantenerlo desensibiliza al equipo a la regla.
[A9] 2026-04-25 19:30 Low segurasist-api/.env.example:52-55 + env.schema.ts:74-77 — falta SQS_QUEUE_INSUREDS_CREATION pese a que workers/insureds-creation-worker.service.ts:63 la usa. // A3/A10 drift env vs runtime.
[A10] 2026-04-25 19:45 High segurasist-api/jest.config.ts:46-48 — `collectCoverageFrom` definido pero NO hay `coverageThreshold` → `test:cov` no falla por regresión. // A1/A2/A4/A5/A6: cualquier área puede regresar sin alarmar al CI.
[A10] 2026-04-25 19:45 High auth.service.spec.ts:93-97 — referencia `test/integration/otp-flow.spec.ts` que NO existe en disco; cubre RF-401 OTP crítico. // A1: cobertura aparente del flow OTP es ilusoria.
[A10] 2026-04-25 19:45 High test/security/cross-tenant.spec.ts:243-285 — 23 it.todo HTTP-layer (insureds 5, batches 3, certs 3, claims 2, packages 2, audit 1, chat 2, reports 1, tenant-override 4); sólo BD blindada. // A1/A2/A4/A5/A6: gate de PR no detecta endpoint que se salte RLS via prismaBypass accidental. (cruza con A2-34, confirmado por A10.)
[A10] 2026-04-25 19:45 Medium test/e2e/insureds-export.e2e-spec.ts:88 — `describe.skip` incondicional pese a 200+ líneas listas; sin nota inline. // A5: regresión silenciosa del export.
[A10] 2026-04-25 19:45 Medium apps/admin/components/insureds/export-button.tsx — componente en uso (page.tsx:23) SIN `*.test.tsx` (removido por hoisting `vi.mock` y no recreado). // A7: cero cobertura FE de exports flow.
[A10] 2026-04-25 19:45 Medium turbo.json:34-37 — `test:unit` con `dependsOn:["^build"]` fuerza build de upstream packages source-mapped (api-client/auth/i18n/ui) → CI lento, cache miss alto. // A9: contribuye al CI time.
[A10] 2026-04-25 19:45 Medium apps/portal/vitest.config.ts:29-37 — sin `thresholds` (admin sí, 80/75/80/80) pese a manejar PII de insureds (F4). // A8: portal puede regresar coverage sin alarma.
[A10] 2026-04-25 19:45 Low PROGRESS.md:97 vs QA_COVERAGE_AUDIT_SPRINT_3.md:12 — counts inconsistentes (454 vs 650 vs 1094 tests). // Cross-cutting: doc fidelity.
[A10] 2026-04-25 19:45 Low LOCAL_DEV.md — NO menciona stale `.next` cache (gap reportado en QA Sprint 3). // A7/A8: dev onboarding tropieza con páginas cacheadas.
[A10] 2026-04-25 19:45 Low CERT_E2E vs CERT_EMAIL_FLOW_E2E env-gates con naming inconsistente (`test/e2e/certificates.e2e-spec.ts:38` vs `test/integration/cert-email-flow.spec.ts:56`); CI no las setea. // A4: dev no sabe cuál activar.
[A10] 2026-04-25 19:45 Low apps/{admin,portal}/.eslintrc.json (42B cada uno) coexisten con eslint.config.mjs flat → dos sistemas según comando (`pnpm lint` vs `next lint`). // A7/A8: drift de reglas posible.
[A4-V2] 2026-04-25 23:00 High pdf-worker.service.ts:305,380 + certificates.service.ts:282,319 — callers a email-queue/pdf-queue NO pasan dedupeId (asimétrico vs A3 insureds-creation, A5 reports-queue que sí lo pasan). // Cross-cut con A3-31, A5-01, A9-09: la decisión FIFO vs idempotencia DB-side debe cubrir TODAS las colas y TODOS los callers.
[A4-V2] 2026-04-25 23:00 Critical [confirmación CORR-01] hash bug A4-01 + iframe CSP A8-03 se enmascaran mutuamente — el caso de uso público "tercero recompute SHA del PDF descargado y verifica QR" está roto end-to-end sin test que lo capture. // A8 (CSP frame-src), A10 (test gap E2E verify recompute).
[A4-V2] 2026-04-25 23:00 Medium certificates.service.ts:209-212 — urlForSelf sin filtro status='issued'; si último cert es failed-as-revoked (A4-03), devuelve URL S3 con s3Key='' malformada al portal. // A8 (UX portal), A4-03 cross-link.
[B9] 2026-04-25 23:30 Critical segurasist-api/prisma/rls/policies.sql:49-64 — exports table NO incluida en array tenant-iso; re-aplicar apply-rls.sh post-migración deja exports SIN RLS habilitado/forzado. // A2-35 confirmado, A5 (reports/exports) impactada.
[B9] 2026-04-25 23:30 Critical segurasist-api/src/workers/insureds-creation-worker.service.ts:63 — String.replace de SQS_QUEUE_LAYOUT funciona en LocalStack; en AWS real falla con QueueDoesNotExist (Terraform no provisiona insureds-creation-queue, env var no existe). // Bloquea Sprint 5 deploy real. Cross con A3-31/32, A9-04/09.
[B9] 2026-04-25 23:30 High segurasist-api/.env.example ↔ env.schema.ts — 8 variables drift (MAILPIT_API_URL, EMAIL_FROM_CERT, CERT_BASE_URL, INSURED_DEFAULT_PASSWORD, OTP_TTL/MAX/LOCKOUT, EMAIL_TRANSPORT/SMTP_*); ENABLE_SWAGGER dead; SQS_QUEUE_INSUREDS_CREATION ausente. // Cross con A6-48 DATABASE_URL_AUDIT, A7-60 API_BASE_URL, A8-51 PORTAL_SESSION_COOKIE.
[B9] 2026-04-25 23:30 High .github/workflows/ci.yml — sin Semgrep custom rules para patrones cross-area (JwtAuthGuard global, webhook throttle, CSRF logout, tenant-throttle consistency). // A1-21, A4-25, A6-49, A7-57.
[B9] 2026-04-25 23:30 High segurasist-infra/envs/{dev,staging}/providers.tf — falta alias us_east_1 (sólo prod). // Drift cuando staging promueva CLOUDFRONT WAF Sprint 5.
[B9] 2026-04-25 23:30 High segurasist-api/src/infra/aws/ses.service.ts:154 — sendViaSes desestima headers con `void _headersUnused`; Configuration Set tags inútil en prod. // A4-23 confirmado; bloquea CW alarms-by-email-type.
[B9] 2026-04-25 23:30 High segurasist-api/scripts/{backup,restore}.sh — sin cross-region replication, sin source-bucket-must-have-object-lock validation, sin --dry-run para drill mensual. // ADR-014 cross-region failover, RB-008 PITR.
[B9] 2026-04-25 23:30 High 9/12 runbooks (RB-001..009/010) con `> TBD` en symptom/diagnosis/recovery + IRP.md esqueleto puro (roles/severities/phases TBD). // LFPDPPP gap si incidente Sprint 4; B9-03 alarms inexistentes amplifica.
[B9] 2026-04-25 23:30 Medium docs/INTERIM_RISKS.md:58-69 — WAF apply diferido a "Sprint 5" sin fecha ISO. // SOC 2 audit gap futuro; AWS-001 ETA bloqueante.
[B9] 2026-04-25 23:30 Medium external/AWS-004.md:26 — Cognito mx-central-1 disponibilidad NO confirmada; fallback us-east-1 contradice Aviso de Privacidad LEG-001. // Sprint 4 abrir AWS support ticket; LEG-002 si aplica.
[A4-V2] 2026-04-25 23:00 Medium ses-webhook.controller.ts:87-97 — fetch(SubscribeURL) sin validación URL en non-prod (SSRF blind) ni AbortController/timeout (cuelga 30s). // A6 webhook hardening; A8-08 mismo patrón sin timeout.
[A4-V2] 2026-04-25 23:00 High ses.service.ts:150-155 — comment afirma "SDK v3 SendEmailCommand NO soporta headers custom" pero Tags SÍ son soportados directo (SendRawEmailCommand sólo necesario para headers MIME custom). Fix trivial bloqueado por comment técnicamente incorrecto. // A4-02 sub-finding; afecta webhook downstream.
