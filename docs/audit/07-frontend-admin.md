# Audit Report — Frontend admin (07)

## Summary (≤10 líneas)

`apps/admin/` (Next.js 14 App Router) está bien estructurado: Server vs Client Components correctamente separados, RBAC server-gated en cada ruta sensible, hardening cookies M6+L3 (`buildSessionCookie` con sameSite=strict + secure allowlist), Origin allowlist enforcement en middleware + handler, JWT decode con guards, y vista 360° (S3-06) cohesiva. Los principales riesgos son: (1) un **callback NextAuth** que sigue usando `setSessionCookies` de `@segurasist/auth` con `sameSite='lax'` (gap H-02 del security audit), bypaseando el endurecimiento local; (2) **duplicación verbatim** de `cookie-config.ts` y `origin-allowlist.ts` entre admin y portal — candidato obvio a `packages/`; (3) **mock tenant select hard-coded** en `mobile-drawer.tsx` que no usa `TenantSwitcher` real (security S3-08). También hay code dead (`kpi-grid.tsx`, `dashboard-tables.tsx`, deep-links legacy del 360°), tests con `process.env` mutation directa, y `framer-motion` sin mock global. El logout con `GET` permite CSRF logout. 191 tests admin, 19 archivos, 155 `it()`/`test()` + 39 describe blocks.

## Files audited (62 source files)

- **App routes** (28): `app/(auth)/{login,callback}/page.tsx`; `app/(app)/{layout,template}.tsx`; `app/(app)/dashboard/{page,dashboard-client,charts,kpi-grid,dashboard-tables}.tsx`; `app/(app)/{insureds,batches,packages,reports,users,settings}/page.tsx`; `app/(app)/insureds/[id]/{page,insured-360-client,datos,coberturas,eventos,certificados,auditoria}.tsx`; legacy stubs `[id]/{coverages,claims,certificates,audit}/page.tsx`.
- **API routes** (4): `app/api/auth/{local-login,me,[...nextauth]}/route.ts`, `app/api/proxy/[...path]/route.ts`.
- **Lib** (7): `lib/{rbac,jwt,auth-server,cookie-config,origin-allowlist}.ts`, `lib/hooks/{use-dashboard,use-insured-360,use-tenant-override,use-debounced-value}.ts`.
- **Components** (12): `app/_components/{access-denied,command-palette,command-k-trigger,sidebar-nav,mobile-drawer,mobile-search-trigger,theme-toggle}.tsx`; `components/{header/tenant-switcher,header/tenant-override-bridge,layout/tenant-override-banner,packages/package-editor,insureds/export-button}.tsx`.
- **Middleware + config**: `middleware.ts`, `next.config.mjs`, `vitest.config.ts`, `app/layout.tsx`, `app/providers.tsx`.
- **Tests** (19 files): `test/unit/{components,lib,api}/*` + `test/setup.ts`, `test/helpers/{cookies,jwt}.ts`.

## Strengths (qué está bien hecho)

1. **Hardening cookies M6+L3** (`lib/cookie-config.ts:18-69`): `buildSessionCookie` centraliza sameSite=strict, httpOnly, y secure con allowlist explícita (`'production'|'staging'`). Test coverage exhaustivo (`cookie-config.test.ts`) incluye casos de drift (`'prod'`, `'production-staging'`, whitespace).
2. **Origin allowlist defense-in-depth** (`lib/origin-allowlist.ts` + `middleware.ts:71-82` + `local-login/route.ts:52-59`): enforcement en middleware + re-check en handler; webhooks excluidos por prefijo.
3. **JWT helpers extraídos y testeables** (`lib/jwt.ts`): `decodeJwtPayload` no verifica firma, comentario explícito ("UX-only"), defensivo ante input malformado, tests cubren edge cases. Production middleware delega a `protectMiddleware` (signature verification real).
4. **Server-only auth fetch** (`lib/auth-server.ts`): `import 'server-only'` + `cookies()` desde `next/headers`; tokens nunca llegan al cliente. Defaults a `null` en errores → degradación graceful.
5. **RBAC matrix UX-only con backend-source-of-truth** (`lib/rbac.ts:1-9`): comentario claro de que es UX-only, secciones tipadas (general/admin), `canAccess` con prefix matching correcto (`${href}/`), `isRole` type guard.
6. **Tenant override Bridge pattern** (`tenant-override-bridge.tsx`): registra getter en `useEffect` para que `@segurasist/api-client` lo lea sin importar Zustand desde el package — separación correcta. Override in-memory (NO localStorage) por security-req S3-08.
7. **Vista 360° cohesiva** (S3-06): single fetch, URL state vía `?tab=`, `notFound()` anti-enumeration en 404, IP masking en auditoría (`maskIp`), Skeleton + Empty + Error states completos.
8. **CSP estricta** en `next.config.mjs`: `frame-ancestors 'none'`, HSTS preload, COOP/COEP, Permissions-Policy.
9. **Skip-link a11y** (`app/layout.tsx:54`) + `aria-label`/`aria-describedby` consistentes en formularios.
10. **TanStack Query staleTime tuning** correcto: `60s` global default, `30s` insured 360°, `5min` tenants list.

## Issues found

| ID | File:line | Severity | Category | Description | Recommendation |
|---|---|---|---|---|---|
| A7-01 | `app/api/auth/[...nextauth]/route.ts:10,68,77-80` | **High** | Security | El callback OAuth usa `setSessionCookies` de `@segurasist/auth/session` que setea `sameSite:'lax'` (no strict). Tras Cognito SSO, la sesión queda con CSRF posture distinto a la del local-login bridge. Bypass del hardening M6 admin. | Migrar callback a `buildSessionCookie` local, o (cross-cutting) hardening en `packages/auth/src/session.ts`. Coordinar con A1/auth fix de H-02. |
| A7-02 | `app/api/auth/[...nextauth]/route.ts:28,77-81,86` | **High** | Security | `logout` se sirve por **GET** (línea 28 dispatch + 77 + alias `POST = GET`). Permite CSRF logout vía `<img src="/api/auth/logout">` o link. Falta `checkOrigin`. | Forzar POST + Origin check. Eliminar `POST = GET` para `logout`; o registrar handler dedicado `logout/route.ts`. |
| A7-03 | `apps/admin/lib/cookie-config.ts` ↔ `apps/portal/lib/cookie-config.ts` y `apps/admin/lib/origin-allowlist.ts` ↔ `apps/portal/lib/origin-allowlist.ts` | **Medium** | Maintainability | `origin-allowlist.ts` es **byte-idéntico** entre admin y portal (`diff` confirma). `cookie-config.ts` es funcionalmente idéntico (sólo cambian comentarios). 4 archivos, 2 lógicas duplicadas. Drift inevitable. | Mover a `packages/security/` o `packages/auth/src/cookie.ts`+`origin.ts`. Cambio coordinado con A8/portal y A1/auth. |
| A7-04 | `app/_components/mobile-drawer.tsx:83-91` | **Medium** | Security/Pattern | Mobile drawer renderiza un `<Select defaultValue="mac">` con opciones hard-coded `Hospitales MAC` / `Demo Tenant`. NO usa `TenantSwitcher` real. Mobile users de admin_segurasist no ven la lista activa, no impersonan, y el banner amber no se sincroniza con su selección. | Reemplazar con `<TenantSwitcher role={role} />` o equivalente bottom-sheet; eliminar el `defaultValue="mac"` hard-coded. |
| A7-05 | `app/(app)/dashboard/kpi-grid.tsx`, `app/(app)/dashboard/dashboard-tables.tsx` | **Low** | Structure | Dead code: `KpiGrid` y `DashboardTables` exportan datos mock pre-S2-05; nadie los importa (`grep` confirma 0 usos). El archivo `kpi-grid.tsx` (88L) y `dashboard-tables.tsx` (143L) duplican lógica del `dashboard-client.tsx` real. | Eliminar ambos archivos. |
| A7-06 | `app/(app)/insureds/[id]/{coverages,claims,certificates,audit}/page.tsx` | **Low** | Structure | Páginas legacy con datos mock — `[id]/page.tsx:8-10` documenta que viven como deep-links pre-S3-06 y "Sprint 4 las consolida o elimina". `certificates/page.tsx` muestra mock data en producción. | Eliminar las 4 rutas legacy en Sprint 4 (ya documentado como deuda). El 360° las cubre todas. |
| A7-07 | `test/unit/lib/origin-allowlist.test.ts:5,8,13,14,77` | **Medium** | Test-coverage / DX | El test muta `process.env['NEXT_PUBLIC_ADMIN_ORIGIN']` directamente (`delete`, asignación). Funciona hoy, pero rompe con TS strict (TS2540) y con `vi.unstubAllEnvs` global. Inconsistente con `cookie-config.test.ts` que sí usa `vi.stubEnv`. | Migrar a `vi.stubEnv('NEXT_PUBLIC_ADMIN_ORIGIN', ...)` + `vi.unstubAllEnvs()` en afterEach. |
| A7-08 | `app/api/proxy/[...path]/route.ts:16` vs `auth-server.ts:6`, `local-login/route.ts:24`, `me/route.ts:14` | **Medium** | Pattern / DX | Inconsistencia de defaults `API_BASE_URL`: el proxy default es `'https://api.segurasist.app'` (prod), los otros 3 default `'http://localhost:3000'` (dev). Si en prod la env var falta, `auth-server.fetchMe` apunta a localhost (falla), proxy va a prod (funciona). | Unificar default vía constante exportada en `lib/api-base.ts` + validar `assertEnv('API_BASE_URL')` al boot en producción. |
| A7-09 | `components/packages/package-editor.tsx:10-12` (comentario) y resto del repo admin | **Low** | Pattern | Comentario "Sin dependencias react-hook-form ni @hookform/resolvers para mantener el bundle ligero". Decisión defensible, pero aplicada solo a este editor. Resto de forms (login, batch wizard) usan plain `useState`. Inconsistencia: el checklist espera RHF + Zod resolver consistente. | Documentar la decisión arquitectónica en `docs/frontend/forms.md` o adoptar RHF en todos los forms grandes (batch wizard, package editor, beneficiarios futuros). |
| A7-10 | `app/(app)/insureds/[id]/eventos.tsx:44-47` | **Low** | UX/A11y | Botón "Nuevo evento" `disabled` sin `aria-disabled` + `title="Próximamente"` ni tooltip explicando por qué. Lo mismo `cert-reissue-btn` en certificados (handler placeholder). | Agregar `title` o `aria-describedby` con copy "Disponible en Sprint 4". Considera `Tooltip`. |
| A7-11 | `app/(app)/insureds/page.tsx` (`'use client'` + `dynamic = 'force-dynamic'` ausente) | **Low** | Performance | Toda la página es client-side; no se aprovecha SSR para los filtros/SEO (no aplica al admin pero sí para perceived perf de primer render). Otros listings (`packages`) sí gatean en server con `fetchMe`. | Considerar gate server-side `canAccess('/insureds', me.role)` antes del client component (consistencia + permite mostrar AccessDenied sin flash). |
| A7-12 | `lib/auth-server.ts:40-48` | **Low** | Maintainability | `MeResponse` define 3 fallback shapes (`body.email ?? body.user?.email ?? body.data?.email`) por "legacy/loose fallbacks". Si la API ya estabilizó el contrato (Sprint 1+2), estos fallbacks introducen complexidad sin valor. | Eliminar fallbacks `body.user.*`/`body.data.*` y dejar solo `body.email/role/tenant`; consultar A1 (auth) para confirmar contrato estable. |
| A7-13 | `app/_components/` (7 files) vs `components/{header,layout,packages,insureds}/` | **Low** | Structure | Dos jerarquías paralelas: shell components viven en `app/_components/` (sidebar, mobile-drawer, command-palette, theme-toggle, access-denied), feature components en `components/`. La distinción no está documentada. Sidebar-nav importa `../../lib/rbac` mientras tenant-switcher importa `../../lib/hooks/use-tenant-override` — ambos cross-boundary. | Documentar regla (e.g. "shell-only en `_components`") o consolidar en `components/`. |
| A7-14 | `next.config.mjs:30-31` | **Low** | Security | CSP en prod incluye `'unsafe-inline'` para `script-src` y `style-src`. Cualquier XSS en server-rendered HTML escala. El Tailwind/Next.js puede operar con nonces/hash-based CSP. | Migrar a CSP con nonces (`Content-Security-Policy` per-request via middleware). Seguir la guía de Next.js 14 strict CSP. |
| A7-15 | `app/(app)/insureds/[id]/auditoria.tsx:104` | **Low** | Pattern | `exportHref = /api/proxy/v1/audit/log?...&format=csv` se renderiza como `<a href>`. La proxy route es GET-friendly y reusa cookie sa_session, pero el CSV inline navigation puede dejar el token en logs de servidor. | OK porque la proxy strip-elimina headers sensibles; sólo flag para confirmar que `format=csv` está soportado por el proxy + audit module. |
| A7-16 | Tests sin `vi.mock('framer-motion', ...)` | **Low** | Test-coverage | El checklist pide mock global de framer-motion. Componentes con `motion.*` (login, dashboard-client, app/template, banner) renderizan animaciones reales en jsdom. No falla pero ralentiza tests + warnings. | Agregar mock global en `test/setup.ts`: `vi.mock('framer-motion', ...)` con stubs `motion.div = forwardRef passthrough`. |
| A7-17 | `package-editor.tsx` y `new-batch-wizard.tsx` no comparten `useDebouncedValue` con `insureds/page.tsx` | **Low** | Maintainability | `packages-client.tsx:62-65` reimplementa el debounce en línea (`setTimeout`); `insureds/page.tsx` usa el hook reusable `useDebouncedValue` (S3-07). | Unificar: usar el hook en `packages-client`. |

## Cross-cutting concerns (apend al feed compartido)

1. **A7 → A1/A8/AUTH**: `packages/auth/src/session.ts` con `sameSite='lax'` aún en uso por `app/api/auth/[...nextauth]/route.ts` (callback Cognito) — bypass del hardening M6+L3 que la admin/portal hicieron en `cookie-config.ts`. Si A1 no fixea H-02, A7 propone: o bien eliminar el callback NextAuth si está deprecated, o usar `buildSessionCookie` también ahí.
2. **A7 → A8 (portal)**: `cookie-config.ts` y `origin-allowlist.ts` byte-idénticos entre admin y portal; promover a `packages/security/` o `packages/auth/src/`.
3. **A7 → A1 (auth)**: contrato `GET /v1/auth/me` consume `body.email` directamente pero `auth-server.ts` aún tolera `user.email`/`data.email`. Si A1 confirma contrato estable, A7 puede limpiar.
4. **A7 → A6 (audit/throttler)**: el `audit-export-link` en `auditoria.tsx:104` apunta a `/v1/audit/log?...&format=csv`. A6 reportó `verify-chain` sin throttle; este export también puede beneficiarse de rate limit explícito.
5. **A7 → A5 (reports/exports)**: `useExportStatus` polling cada 2s en `export-button.tsx`. Si A5 estabiliza la cola FIFO, considerar SSE/WebSocket para no martillar el endpoint.

## Recommendations Sprint 4

1. **(High) Fix CSRF logout + lax cookie callback** — A7-01 + A7-02. Reemplazar `setSessionCookies` por `buildSessionCookie` en `[...nextauth]/route.ts`; obligar POST + origin check en `logout`. Coordinar fix de `packages/auth/src/session.ts` con A1.
2. **(High) Tenant switcher móvil real** — A7-04. El admin_segurasist en mobile actualmente NO puede impersonar correctamente (el select hard-coded confunde el banner amber). Riesgo operativo, no solo cosmético.
3. **(Medium) Dedupe security helpers a `packages/`** — A7-03. Mover `cookie-config.ts` + `origin-allowlist.ts` a un package shared con tests parametrizados; reduce drift admin↔portal.
4. **(Medium) Limpieza dead code** — A7-05 + A7-06. Eliminar `kpi-grid.tsx`, `dashboard-tables.tsx`, y las 4 páginas mock legacy de `insureds/[id]/{coverages,claims,certificates,audit}/`.
5. **(Low) Test hardening** — A7-07 (`vi.stubEnv` en origin-allowlist test) + A7-16 (mock framer-motion en setup).
6. **(Low) Tooltip + aria en botones disabled** — A7-10. Pequeño polish de a11y antes de Go-Live.
