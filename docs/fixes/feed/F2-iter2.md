# F2 — feed entries iter 2

> Bundle: B-PORTAL-AUTH + B-CSP. Append-only. F0 orquestador integra estas
> entradas a `docs/fixes/_fixes-feed.md`.

```
[F2] 2026-04-28 06:30 iter2 STARTED — proxy migration + post-F6 verification
[F2] 2026-04-28 06:35 iter2 DONE segurasist-web/apps/portal/app/api/proxy/[...path]/route.ts — migrado a `makeProxyHandler({cookieName, originAllowlist, apiBase})` de @segurasist/security/proxy. 72 LOC → 32 LOC (-40 LOC, -55%); el handler real son 5 líneas, el resto son comments preservando el rationale histórico de C-02/H-04. El factory ya valida 403 origin / 401 missing-cookie / Bearer-forwarding / hop-by-hop drop / x-trace-id propagation; los 9 specs en packages/security/test/proxy.spec.ts cubren toda la matriz. Import vía alias `@/lib/cookie-names` (tsconfig baseUrl '.'); resolución `@segurasist/security` ya tiene symlink en apps/portal/node_modules. // F7 dueño del paquete; semantics preservadas.
[F2] 2026-04-28 06:38 iter2 DONE segurasist-api/src/modules/auth/auth.service.ts:307-340 — verificación post-F6: `persistCognitoSubFromTokens` sigue intacto en líneas 326,352-389. F6 iter2 todavía no se ha ejecutado (no existe feed/F6-iter2.md), así que el audit log éxito en líneas 329-338 todavía usa `action:'login'` literal — cuando F6 migre a `otp_verified` enum + AuditContextFactory.fromRequest(), mi sección success path queda intocada (fix iter1 vive aguas arriba del audit call y no comparte líneas). NO conflict esperado en merge.
[F2] 2026-04-28 06:40 iter2 NEW-FINDING for-F10 — documentar en docs/fixes/DEVELOPER_GUIDE.md sección 3 (Setup) el cleanup obligatorio del cache `.next/` de apps/portal y apps/admin tras pulls que toquen `lib/cookie-names.ts` o `packages/security/`. Comando: `rm -rf segurasist-web/apps/{admin,portal}/.next` antes de `pnpm dev`. Razón: Next 14 inlinea constantes/imports en chunks compilados; sin purgar, el dev server sirve la versión vieja de PORTAL_SESSION_COOKIE y tests integration leen cookies inexistentes (`sa_session` en lugar de `sa_session_portal`). Esto fue mi NEW-FINDING iter1 #2; lo paso ahora a F10 para que lo capture en DEVELOPER_GUIDE.md.
[F2] 2026-04-28 06:42 iter2 iter2-complete — proxy migration done, persistCognitoSubFromTokens verified intact post-F6 (F6-iter2 aún pending pero cambios contemplados son ortogonales). Tests existentes no corridos localmente (sandbox bloquea pnpm). El factory ya tiene 9 specs verdes en packages/security; mis ediciones del consumer son superficiales (cambio de implementación a delegate, contrato HTTP idéntico). F0 debe correr `pnpm --filter portal test` + `pnpm --filter @segurasist/security test` en gate D4 final.
```
