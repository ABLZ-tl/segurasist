## F8 iter 2 — DevOps follow-ups (terraform-plan workflow + RB-011/012 + EMF verify)

[F8] 2026-04-28 08:00 iter2 STARTED — terraform-plan workflow + runbook renumbering + EMF verify + RB-014 coordination con F5.

[F8] 2026-04-28 08:15 iter2 DONE .github/workflows/terraform-plan.yml — created with OIDC for dev/staging/prod. 3 jobs (`plan-{dev,staging,prod}`) consumiendo `secrets.TF_PLAN_{DEV,STAGING,PROD}_ROLE_ARN` (que mapean a los outputs `tf_role_arns.plan_{dev,staging,prod}` de `iam-github-oidc/outputs.tf` creados en iter 1). Cada job corre `terraform fmt -check`, `init -backend=false`, `validate`, y `plan -no-color`; sube el plan como artifact + comenta el diff en el PR (truncado a 60k chars). Aggregate gate `plan-success` para PR branch protection (single required check). `paths` filter en `segurasist-infra/**` para no correr en PRs sólo de docs/web. Concurrency cancel-in-progress por ref. Region `mx-central-1` hardcoded; us-east-1 alarms en prod usan aliased provider y NO requieren job dedicado. Cierra el NEW-FINDING de F8 iter 1 (workflow ausente referenciado en C-13 audit).

[F8] 2026-04-28 08:30 iter2 DONE RB-011 + RB-012 created — coordinación con F10 vía feed completada. Slots reasignados según el audit Sprint 5:
  - `RB-011-batch-stuck-processing.md` (NUEVO) — batches en `validating` o `processing` > 15 min. 3 flujos triage (A: layout-worker stuck, B: insureds-creation backpressure, C: bug worker no transiciona a `completed` post-ack). Triggered by `sqs-{layout,insureds-creation}-dlq-depth > 0`.
  - `RB-012-pdf-generation-backlog.md` (NUEVO) — pdf queue depth crece más rápido que Lambda throughput. 3 flujos triage (A: capacity / concurrency cap, B: render failing por template/KMS throttle, C: Lambda timeout). Triggered by `sqs-pdf-dlq-depth > 0` o `lambda-pdf_renderer-errors > 0`.
  - **Renumeración previas** (resolver collision con audit Sprint 5): `RB-011-dast-failure.md` → `RB-015-dast-failure.md`; `RB-012-waf-rules.md` → `RB-016-waf-rules.md`. Headers + nota numeración actualizados en ambos. Cross-refs en 4 archivos corregidos (`waf-managed-rules.md` ×3, `RB-005-waf-spike.md` ×2, `modules/waf-web-acl/README.md` ×1).

[F8] 2026-04-28 08:35 iter2 DONE alarms.tf metrics namespace verified post-F6 — las 3 alarmas custom (`audit-writer-degraded`, `audit-mirror-lag`, `audit-chain-tampering`) ya referencian `namespace = "SegurAsist/Audit"` con metric_name `AuditWriterHealth` / `MirrorLagSeconds` / `AuditChainValid` y dimension `Environment = var.environment` en los 3 envs. Esto coincide con lo que F6 emitirá vía CloudWatch EMF en iter 2 según mi NEW-FINDING de iter 1. NO requiere cambio en `alarms.tf`. Cuando F6 cierre la emisión, las alarmas saldrán de INSUFFICIENT_DATA automáticamente.

[F8] 2026-04-28 08:40 iter2 DONE alarms.tf runbook routing — al renumerar RB-011/012 corregí los tags `Runbook` para que la página on-call lleve directo al runbook correcto (en lugar del genérico RB-004/RB-007):
  - `alarm_sqs_dlq_depth` (3 envs): introducido `local.queue_runbooks` map → `layout`/`insureds-creation` → `RB-011`, `pdf` → `RB-012`, `emails`/`reports` → `RB-004`. Description y tag `Runbook` ahora usan `lookup(local.queue_runbooks, each.key, "RB-004")`.
  - `alarm_lambda_errors` (3 envs): `local.lambda_functions` ahora es objeto `{name, runbook}` → `pdf_renderer` → `RB-012`, `emailer` → `RB-004`, `audit_export` → `RB-007`. `dimensions = { FunctionName = each.value.name }`.
  - Tag `Severity = "P1-Security"` en `audit-chain-tampering` se mantiene (intencional, RB-013).

[F8] 2026-04-28 08:45 iter2 NOTE — pre-Sprint5 apply: el SQS topic rename (`<env>-certificates` → `<env>-pdf` finding F5 iter 1) requiere drain ordenado de la cola vieja antes de destruir; el procedimiento vive en `RB-014-sqs-topic-rename-drain.md` (owner F5 iter 2). Mi `terraform apply` para alarms.tf NO toca SQS y es seguro de aplicar antes; pero cualquier apply combinado que incluya `local.queues` rename DEBE seguir RB-014 — anotado aquí para que F0 orquestador lo encadene correctamente.

[F8] 2026-04-28 08:50 iter2 NOTE — coordinación con F10 iter 2 cerrada. F10 documentó "RB-011/012 quedan deferred a Sprint 5+ con slots RB-015+ libres" basándose en el estado pre-dispatch; el dispatch iter 2 sobre-escribió esa decisión por requerimiento explícito del audit Sprint 5. Net: F8 hace la renumeración (DAST→RB-015, WAF→RB-016) + crea los RB-011/012 nuevos; F10 ya cerró RB-009 (KMS) + RB-010 (IRP) y NO se toca esa porción. Listado actualizado de runbooks: 001 (api-down) F8 · 002 (rds-cpu) F8 · 003 (failover) TBD · 004 (sqs-dlq) F8 · 005 (waf-spike) F8 · 006 (guardduty) TBD · 007 (audit-degraded) F8 · 008 (rds-pitr) TBD · 009 (kms-rotation) F10 · 010 (irp-p1) F10 · 011 (batch-stuck) F8 iter2 · 012 (pdf-backlog) F8 iter2 · 013 (audit-tampering) F8 · 014 (sqs-rename-drain) F5 iter2 · 015 (dast-failure) F8 · 016 (waf-rules) F8.

[F8] 2026-04-28 08:55 iter2 NEW-FINDING — `terraform-plan.yml` requiere 3 GitHub repo secrets nuevos: `TF_PLAN_DEV_ROLE_ARN`, `TF_PLAN_STAGING_ROLE_ARN`, `TF_PLAN_PROD_ROLE_ARN` (mapean a `tf_role_arns.plan_{dev,staging,prod}` outputs). F0 orquestador / DevOps lead debe poblarlos en `gh secret set` después del primer `terraform apply` de `iam-github-oidc/`; sin ellos los 3 jobs fallan con AssumeRoleWithWebIdentity error. Documentado en cabecera del workflow.

[F8] 2026-04-28 09:00 iter2 iter2-complete — terraform-plan workflow creado (3 envs OIDC + aggregate gate); RB-011/012 nuevos del audit + 2 runbooks renumerados (DAST→015, WAF→016) sin pérdida de contenido; alarms.tf runbook tags refinados por-queue y por-lambda; namespace `SegurAsist/Audit` confirmado consistente con F6's EMF plan (sin cambio en alarms.tf). 1 NEW-FINDING (3 GH secrets a poblar). Tests no aplicables (workflows + Terraform + Markdown; `terraform fmt -check` y `validate` corren dentro del propio workflow al primer PR).
