// Empty module stub. Aliased in vitest.config.ts in place of `server-only`
// (Next.js' fail-fast guard against importing server modules from clients).
// Vitest doesn't ship a client/server boundary, so a no-op is correct here.
export {};
