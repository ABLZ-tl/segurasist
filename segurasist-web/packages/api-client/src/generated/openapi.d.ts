/**
 * Generated OpenAPI types — placeholder.
 *
 * Run `pnpm --filter @segurasist/api-client openapi:gen` against the running
 * backend (default `https://api.segurasist.app/openapi.json`, override with
 * `OPENAPI_URL=...`) to overwrite this file with the real `paths` and
 * `components` from `openapi-typescript`.
 *
 * Until that runs, hand-written types in `../types.ts` are the source of
 * truth for hooks. Do not edit this stub by hand.
 */

export interface paths {}
export interface components {
  schemas: Record<string, unknown>;
}
export interface operations {}
